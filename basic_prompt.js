/**
 * Write a function called `toCamelCase` that takes a string as input and returns the camelCase version of that string.
 * The function should:
 * - Remove any non-alphanumeric characters (except spaces).
 * - Capitalize the first letter of each word except the first word.
 * - Concatenate all words together without spaces.
 * 
 * Example:
 * toCamelCase("hello world") // returns "helloWorld"
 * toCamelCase("Convert THIS to camel case!") // returns "convertThisToCamelCase"
 */